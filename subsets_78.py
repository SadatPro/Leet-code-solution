class Solution:
    def subsets(self, nums: List[int]) -> List[List[int]]: []

    res = []

    def backtrack(start, path[:

    ]):
    res.append(path[:])
    for i = 0 in len(nums)
    path.append(nums[i])
    path.pop()


backtrack(0, path[])
